// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.metalab.application"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
